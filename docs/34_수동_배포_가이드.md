# NeuroNova 수동 배포 가이드

## 📋 배포 프로세스

```
1. 서버에서 git clone
2. 필요한 파일 3가지 업로드
3. 배포 스크립트 실행
```

---

## 📦 필요한 파일 목록

### 1. .env 파일 (환경 변수)
### 2. ML 모델 파일
### 3. Pickle 파일 (예시 데이터)

---

## 1️⃣ .env 파일

### 📍 업로드 위치
```
backend/django_main/.env
```

### 📄 파일 내용 (템플릿)

```bash
# ============================================
# Django 기본 설정
# ============================================
DEBUG=False
SECRET_KEY=your-production-secret-key-change-this-to-random-string
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,서버IP주소

# ============================================
# 데이터베이스 (MySQL)
# ============================================
DB_ENGINE=django.db.backends.mysql
DB_NAME=neuronova_db
DB_USER=neuronova_user
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=3306

# ============================================
# Redis (캐싱)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# ============================================
# CORS 설정
# ============================================
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ============================================
# CSRF 설정
# ============================================
CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ============================================
# 외부 API 키
# ============================================

# DrugBank API (약물 정보)
DRUGBANK_API_KEY=

# AlphaFold API (단백질 구조)
ALPHAFOLD_API_KEY=

# PubChem API (화합물 정보) - 선택적
PUBCHEM_API_KEY=

# ============================================
# Firebase/FCM (푸시 알림)
# ============================================
FIREBASE_SERVER_KEY=
FIREBASE_PROJECT_ID=

# ============================================
# 내부 서비스 키
# ============================================

# ML API 서비스 키
ML_SERVICE_KEY=your-ml-service-secret-key
ML_API_URL=http://localhost:5001

# ============================================
# 암호화 키
# ============================================

# 환자 주민등록번호 암호화 키
# 생성: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-32-byte-base64-encoded-encryption-key

# ============================================
# JWT 설정
# ============================================
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# ============================================
# Flask AI Inference Server
# ============================================
FLASK_INFERENCE_URL=http://127.0.0.1:9000
FLASK_API_KEY=your-flask-api-key

# ============================================
# Orthanc DICOM Server
# ============================================
ORTHANC_URL=http://localhost:8042
ORTHANC_USERNAME=orthanc
ORTHANC_PASSWORD=orthanc

# ============================================
# 이메일 설정 (선택)
# ============================================
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password

# ============================================
# 의료진 가입 설정
# ============================================
# 개발: True, 프로덕션: False
ALLOW_PRIVILEGED_SIGNUP=False
REQUIRE_PRIVILEGED_APPROVAL=True

# ============================================
# 환자 데이터 만료 (일)
# ============================================
PATIENT_DATA_EXPIRATION_DAYS=90

# ============================================
# 프로젝트 정보
# ============================================
PROJECT_NAME=NeuroNova
PROJECT_VERSION=1.0.0
API_VERSION=v1
```

### 🔐 필수 값 설정 (반드시 변경!)

```bash
# 1. SECRET_KEY 생성
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# 2. ENCRYPTION_KEY 생성
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# 3. 나머지는 실제 값으로 변경
```

### 📤 업로드 방법

**Option 1: SCP로 업로드**
```bash
# 로컬에서 실행
scp .env ubuntu@서버IP:/var/www/neuronova/backend/django_main/.env
```

**Option 2: 서버에서 직접 생성**
```bash
# 서버에서 실행
ssh ubuntu@서버IP
cd /var/www/neuronova/backend/django_main
nano .env
# 위 내용 붙여넣기
# Ctrl+O → Enter → Ctrl+X
```

---

## 2️⃣ ML 모델 파일

### 📍 업로드 위치
```
backend/flask_ml/models/
```

### 📂 필요한 모델 파일 목록

```
backend/flask_ml/models/
├── xray_pneumonia_model.h5           # X-ray 폐렴 진단 모델 (주 모델)
├── xray_pneumonia_model_weights.h5   # 모델 가중치 (선택)
├── ct_scan_model.h5                  # CT 스캔 모델 (있다면)
└── metadata.json                      # 모델 메타데이터 (선택)
```

### 📏 파일 크기 예상
- **xray_pneumonia_model.h5**: ~50-200MB
- 기타 모델: 각 50-500MB

### 📤 업로드 방법

**Option 1: SCP로 업로드**
```bash
# 로컬에서 실행
scp backend/flask_ml/models/*.h5 ubuntu@서버IP:/var/www/neuronova/backend/flask_ml/models/
```

**Option 2: SFTP로 업로드**
```bash
# SFTP 접속
sftp ubuntu@서버IP

# 디렉토리 이동
cd /var/www/neuronova/backend/flask_ml/models

# 파일 업로드
put backend/flask_ml/models/xray_pneumonia_model.h5

# 종료
exit
```

**Option 3: rsync로 업로드 (권장 - 대용량)**
```bash
# 로컬에서 실행
rsync -avz --progress \
  backend/flask_ml/models/*.h5 \
  ubuntu@서버IP:/var/www/neuronova/backend/flask_ml/models/
```

### ⚠️ 모델 파일이 없는 경우

모델 파일이 아직 없다면:

```bash
# 서버에서 더미 파일 생성 (테스트용)
ssh ubuntu@서버IP
cd /var/www/neuronova/backend/flask_ml/models
touch xray_pneumonia_model.h5

# 또는 사전 훈련된 모델 다운로드
# wget https://your-model-storage.com/xray_model.h5
```

---

## 3️⃣ Pickle 파일 (예시 데이터)

### 📍 업로드 위치
```
backend/django_main/fixtures/
또는
backend/flask_ml/data/
```

### 📂 필요한 Pickle 파일 목록

```
backend/django_main/fixtures/
├── sample_patients.pkl          # 샘플 환자 데이터
├── sample_medical_records.pkl   # 샘플 진료 기록
└── sample_medications.pkl       # 샘플 약물 데이터

backend/flask_ml/data/
├── preprocessing_scaler.pkl     # 전처리 스케일러
├── label_encoder.pkl            # 라벨 인코더
└── feature_names.pkl            # 피처 이름 목록
```

### 📏 파일 크기 예상
- 각 파일: ~1-50MB

### 📤 업로드 방법

**Option 1: SCP로 업로드**
```bash
# Django fixtures
scp backend/django_main/fixtures/*.pkl \
  ubuntu@서버IP:/var/www/neuronova/backend/django_main/fixtures/

# Flask ML data
scp backend/flask_ml/data/*.pkl \
  ubuntu@서버IP:/var/www/neuronova/backend/flask_ml/data/
```

**Option 2: 서버에서 직접 생성**
```bash
# 서버에서
ssh ubuntu@서버IP
cd /var/www/neuronova/backend/django_main
python manage.py shell

# Python shell에서
import pickle

# 샘플 데이터 생성 예시
sample_data = {
    'patients': [...],
    'records': [...]
}

with open('fixtures/sample_patients.pkl', 'wb') as f:
    pickle.dump(sample_data, f)
```

### ⚠️ Pickle 파일이 없는 경우

없어도 실행 가능하지만, 데이터 로드 시 에러 발생 가능:

```python
# backend/django_main/apps/core/management/commands/load_sample_data.py

import pickle
import os

try:
    with open('fixtures/sample_patients.pkl', 'rb') as f:
        data = pickle.load(f)
except FileNotFoundError:
    print("Warning: sample_patients.pkl not found. Skipping...")
    data = None
```

---

## 🚀 배포 스크립트

### 📍 위치
```
deploy/scripts/manual_deploy.sh
```

### 📄 스크립트 내용

```bash
#!/bin/bash

# ============================================
# NeuroNova 수동 배포 스크립트
# ============================================

set -e  # 에러 발생 시 중단

echo "===== NeuroNova 수동 배포 시작 ====="

# ============================================
# 1. 환경 확인
# ============================================

echo "1. 환경 확인 중..."

# 현재 디렉토리 확인
if [ ! -d "/var/www/neuronova" ]; then
    echo "❌ 오류: /var/www/neuronova 디렉토리가 없습니다."
    echo "먼저 git clone을 실행하세요."
    exit 1
fi

cd /var/www/neuronova

# .env 파일 확인
if [ ! -f "backend/django_main/.env" ]; then
    echo "❌ 오류: .env 파일이 없습니다."
    echo "backend/django_main/.env 파일을 업로드하세요."
    exit 1
fi

echo "✅ 환경 확인 완료"

# ============================================
# 2. Python 가상환경 설정
# ============================================

echo "2. Python 가상환경 설정 중..."

cd backend/django_main

# 가상환경이 없으면 생성
if [ ! -d "venv" ]; then
    echo "가상환경 생성 중..."
    python3 -m venv venv
fi

# 가상환경 활성화
source venv/bin/activate

# 패키지 업데이트
pip install --upgrade pip

# 의존성 설치
echo "Django 의존성 설치 중..."
pip install -r requirements.txt

echo "✅ Python 설정 완료"

# ============================================
# 3. 데이터베이스 설정
# ============================================

echo "3. 데이터베이스 설정 중..."

# 마이그레이션
python manage.py makemigrations
python manage.py migrate

echo "✅ 데이터베이스 설정 완료"

# ============================================
# 4. 정적 파일 수집
# ============================================

echo "4. 정적 파일 수집 중..."

python manage.py collectstatic --noinput

echo "✅ 정적 파일 수집 완료"

# ============================================
# 5. React 프론트엔드 빌드
# ============================================

echo "5. React 빌드 중..."

cd ../../frontend/react_web

# Node 모듈 설치
if [ ! -d "node_modules" ]; then
    echo "Node 모듈 설치 중..."
    npm install
fi

# React 빌드
npm run build

echo "✅ React 빌드 완료"

# ============================================
# 6. ML 모델 확인
# ============================================

echo "6. ML 모델 확인 중..."

cd ../../backend/flask_ml

# 모델 디렉토리 생성
mkdir -p models

# 모델 파일 확인
if [ ! -f "models/xray_pneumonia_model.h5" ]; then
    echo "⚠️  경고: ML 모델 파일이 없습니다."
    echo "models/xray_pneumonia_model.h5 파일을 업로드하세요."
else
    echo "✅ ML 모델 확인 완료"
fi

# ============================================
# 7. Pickle 파일 확인
# ============================================

echo "7. Pickle 파일 확인 중..."

cd ../django_main

# Fixtures 디렉토리 생성
mkdir -p fixtures

if [ -f "fixtures/sample_patients.pkl" ]; then
    echo "✅ Pickle 파일 확인 완료"
else
    echo "⚠️  경고: Pickle 파일이 없습니다."
    echo "fixtures/*.pkl 파일을 업로드하세요 (선택사항)."
fi

# ============================================
# 8. 디렉토리 권한 설정
# ============================================

echo "8. 권한 설정 중..."

cd /var/www/neuronova

# 소유자 변경
sudo chown -R ubuntu:ubuntu /var/www/neuronova

# Static/Media 디렉토리 권한
sudo chmod -R 755 backend/django_main/staticfiles
sudo mkdir -p backend/django_main/media
sudo chmod -R 755 backend/django_main/media

# 로그 디렉토리 생성
sudo mkdir -p /var/log/neuronova
sudo chown -R ubuntu:ubuntu /var/log/neuronova

echo "✅ 권한 설정 완료"

# ============================================
# 9. Gunicorn 설정
# ============================================

echo "9. Gunicorn 설정 중..."

# Gunicorn 소켓 파일
sudo cat > /etc/systemd/system/gunicorn_django.socket << 'EOF'
[Unit]
Description=gunicorn socket

[Socket]
ListenStream=/run/gunicorn_django.sock

[Install]
WantedBy=sockets.target
EOF

# Gunicorn 서비스 파일
sudo cat > /etc/systemd/system/gunicorn_django.service << 'EOF'
[Unit]
Description=gunicorn daemon for Django
Requires=gunicorn_django.socket
After=network.target

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/var/www/neuronova/backend/django_main
ExecStart=/var/www/neuronova/backend/django_main/venv/bin/gunicorn \
          --access-logfile /var/log/neuronova/gunicorn_access.log \
          --error-logfile /var/log/neuronova/gunicorn_error.log \
          --workers 5 \
          --bind unix:/run/gunicorn_django.sock \
          neuronova.wsgi:application

[Install]
WantedBy=multi-user.target
EOF

# 서비스 활성화
sudo systemctl daemon-reload
sudo systemctl enable gunicorn_django.socket
sudo systemctl enable gunicorn_django.service

echo "✅ Gunicorn 설정 완료"

# ============================================
# 10. Nginx 설정
# ============================================

echo "10. Nginx 설정 중..."

# Nginx 설정 파일 복사
sudo cp deploy/nginx/neuronova.conf /etc/nginx/sites-available/neuronova

# 심볼릭 링크 생성
sudo ln -sf /etc/nginx/sites-available/neuronova /etc/nginx/sites-enabled/

# 기본 사이트 비활성화
sudo rm -f /etc/nginx/sites-enabled/default

# Nginx 설정 테스트
sudo nginx -t

echo "✅ Nginx 설정 완료"

# ============================================
# 11. 서비스 시작
# ============================================

echo "11. 서비스 시작 중..."

# Gunicorn 시작
sudo systemctl start gunicorn_django
sudo systemctl status gunicorn_django --no-pager

# Nginx 재시작
sudo systemctl restart nginx
sudo systemctl status nginx --no-pager

echo "✅ 서비스 시작 완료"

# ============================================
# 12. 배포 확인
# ============================================

echo "12. 배포 확인 중..."

# Health check
sleep 3
response=$(curl -s -o /dev/null -w "%{http_code}" http://localhost/api/health/)

if [ "$response" = "200" ]; then
    echo "✅ Health check 성공 (200 OK)"
else
    echo "⚠️  경고: Health check 실패 (HTTP $response)"
fi

# ============================================
# 완료
# ============================================

echo ""
echo "====================================="
echo "✅ NeuroNova 배포 완료!"
echo "====================================="
echo ""
echo "다음 단계:"
echo "1. 브라우저에서 http://서버IP 접속"
echo "2. 관리자 계정 생성: python manage.py createsuperuser"
echo "3. 로그 확인: tail -f /var/log/neuronova/gunicorn_error.log"
echo ""
```

### 📤 스크립트 업로드 및 실행

```bash
# 1. 스크립트 업로드
scp deploy/scripts/manual_deploy.sh ubuntu@서버IP:/var/www/neuronova/

# 2. 서버에서 실행
ssh ubuntu@서버IP
cd /var/www/neuronova
chmod +x manual_deploy.sh
sudo ./manual_deploy.sh
```

---

## 📝 배포 체크리스트

### 배포 전 준비

- [ ] 서버에 git clone 완료
- [ ] .env 파일 생성 및 업로드
- [ ] ML 모델 파일 업로드
- [ ] Pickle 파일 업로드 (선택)

### 필수 파일 확인

```bash
# 서버에서 확인
cd /var/www/neuronova

# .env 파일
ls -lh backend/django_main/.env

# ML 모델
ls -lh backend/flask_ml/models/*.h5

# Pickle 파일
ls -lh backend/django_main/fixtures/*.pkl
```

### 배포 후 확인

- [ ] 서비스 실행 확인: `sudo systemctl status gunicorn_django`
- [ ] Nginx 실행 확인: `sudo systemctl status nginx`
- [ ] Health check: `curl http://localhost/api/health/`
- [ ] 브라우저 접속: `http://서버IP`

---

## 🔧 트러블슈팅

### 1. .env 파일 권한 에러

```bash
# 권한 수정
cd /var/www/neuronova/backend/django_main
chmod 600 .env
chown ubuntu:ubuntu .env
```

### 2. 모델 파일 로드 실패

```bash
# 파일 확인
ls -lh backend/flask_ml/models/

# 권한 확인
chmod 644 backend/flask_ml/models/*.h5
```

### 3. Pickle 파일 에러

```python
# Django shell에서 테스트
python manage.py shell

import pickle
with open('fixtures/sample_patients.pkl', 'rb') as f:
    data = pickle.load(f)
    print(data)
```

---

**문서 버전**: 1.0
**최종 수정일**: 2025-12-07
**작성자**: NeuroNova 개발팀
